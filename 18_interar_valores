#18 interar valores
data.head(10)

#metodo lento
#indicado para pequenos conjuntos de dados

#para cada index e linha dentro do interrow, para o nosso dataset
for index, row in data.head(10).iterrows():
    print(f'indice {index}, ==> {row["ESTADO"]}')
